[distutils]
index-servers =
    pypi

[pypi]
username: odlgroup

# Extra index server doesn't work for some reason, instructions here instead.
#
# 1. Upload to testpypi
#
#     twine upload --repository-url https://test.pypi.org/legacy/ dist/*
#
# 2. Test the package
#
#     pip install --index-url https://test.pypi.org/simple/ dipp
#
